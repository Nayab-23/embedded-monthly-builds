# Manifests

Each project manifest is the machine-readable source of truth for:

- completion stage and estimated percent complete
- what is implemented, missing, and deferred
- continuation priority and mode
- bounded backlog items for AI-assisted execution
- repo-local validation commands

Each file under `manifests/projects/` is the authoritative metadata record for one weekly project repo.
