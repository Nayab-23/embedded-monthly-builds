# Current Month Plan

This is the reconciled month plan after auditing the real state of the Raspberry Pi and GitHub repos. The original month does not reset; unfinished work rolls directly into the continuation month.

## Status Overview

| Week | Project | Status | Day Equivalent | Notes |
| --- | --- | --- | --- | --- |
| 1 | `embedded-linux-device-supervisor` | Late-stage | 84% | Strong MVP exists; remaining work is polish, soak testing, and recovery-hook verification |
| 2 | `pi-edge-vision-event-detector` | Late-stage | 79% | Strong MVP exists; remaining work is retention visibility and performance polish |
| 3 | `pico-rtos-sensor-hub` | Mid-build | 58% | Host/simulator path is strong; real hardware and deeper firmware validation remain |
| 4 | `peripheral-protocol-workbench` | Early-stage | 28% | Core protocol scaffold exists; serial tooling and stream parsing remain |

## Current Focus

Current active continuation project:

- `pico-rtos-sensor-hub`

Current selected backlog item:

- `stream-parser`

Continuation policy:

- no month reset
- unfinished repos remain backlog targets
- late-stage repos move into polish and verification mode
- the daily runner selects bounded backlog items from manifests and uses the OpenAI API first
